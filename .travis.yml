language: java
jdk:
  - openjdk8

branches:
  only:
    - master

before_install:
  sudo chmod +x gradlew
# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'

script: "./gradlew clean build"

notifications:
  email:
    recipients:
      - snowwwww12@gmail.com  